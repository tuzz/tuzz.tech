import css from "./styles.scss";

const NavBar = () => (
  <div className={css.nav_bar}>
    I'm still moving content over to this site so bear with me. Check back in a few days.
  </div>
);

export default NavBar;
